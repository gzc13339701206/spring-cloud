spring:
  application:
    name: microservice-foo
  cloud:
    config:
      uri: http://localhost:7000/
      profile: dev
      label: master

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
